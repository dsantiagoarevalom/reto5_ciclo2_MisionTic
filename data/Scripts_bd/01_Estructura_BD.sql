CREATE TABLE notas
(
	idnota BIGINT AUTO_INCREMENT NOT NULL,
	nombreestudiante VARCHAR(200) NOT NULL,
	nota1 DOUBLE PRECISION NOT NULL,
	nota2 DOUBLE PRECISION NOT NULL,
	nota3 DOUBLE PRECISION NOT NULL,
	definitiva DOUBLE PRECISION NOT NULL,
	PRIMARY KEY(idnota)
);